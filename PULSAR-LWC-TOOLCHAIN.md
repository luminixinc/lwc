# Pulsar LWC Toolchain

Building custom Pulsar LWC Toolchain (targeting custom Rollupjs SPA).

## Fresh build (rebasing to upstream)

`rm -rf node_modules`
`npx yarn install`

## Iterating

`npx yarn build`
